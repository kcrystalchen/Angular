import { TitleCaseClassHero } from './titleCaseClassHero';

describe('titleCaseClassHero', () => {
  it('should create an instance', () => {
    expect(new TitleCaseClassHero()).toBeTruthy();
  });
});
